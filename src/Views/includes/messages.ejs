<% if(errors.length > 0){ %>

<div class="row">
  <div class="col my-3">
    <div class="alert alert-danger">
      <% errors.forEach(element => { %> <%= element %>
      <br />
      <% }); %>
    </div>
  </div>
</div>
<% } %> <% if(sucess.length>0){ %>
<div class="row">
  <div class="col my-3">
    <div class="alert alert-success"> <%= sucess %> </div>
  </div>
</div>

<% } %>
